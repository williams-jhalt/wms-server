{% use "bootstrap_3_layout.html.twig" %}

{% block form_row -%}
    {%- set widget_attr = {} -%}
    <div class="row">
        <div class="form-group">
            {{- form_label(form) -}}
            {{- form_widget(form, widget_attr) -}}
        </div>
    </div>
{% endblock form_row %}